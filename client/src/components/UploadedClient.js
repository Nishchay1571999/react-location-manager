import React from 'react'

function UploadedClient(props) {
  // const createMarkup = (html) => {
  //   return {
  //     __html: DOMPurify.sanitize(html),
  //   };
  // };

  return (
    <div>UploadedClient</div>
  )
}

export default UploadedClient